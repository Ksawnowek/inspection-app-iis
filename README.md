# Inspection App – IIS, Service + Repository, MSSQL (procs & views)
- Backend FastAPI (warstwy: repo -> service -> API) – MSSQL przez **procedury & widoki**
- Frontend React TS – podpis, dynamiczne parametry
- SQL: tabele, widoki, procedury
- Windows Service (NSSM) + IIS (ARR) reverse proxy
